<%= form_for(@mng) do |f| %>
  <% if @mng.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mng.errors.count, "error") %> prohibited this mng from being saved:</h2>

      <ul>
      <% @mng.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %><br>
    <%= f.text_area :question %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
